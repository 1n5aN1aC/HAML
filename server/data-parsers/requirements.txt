# Dependencies for every importer driven by run_importers.py.
#
# Install into a virtualenv at this directory:
#     python -m venv .venv
#     .venv\Scripts\python -m pip install -r requirements.txt
#
# Use `python -m pip` rather than `pip.exe`: a venv's console-script shims
# hardcode an absolute path to the interpreter that created them, so they keep
# targeting the original location if the folder is ever copied or renamed.

# Core: every download + build + geocode phase of all importers.
requests>=2.31

# Point-in-polygon phases.
shapely>=2.0
pyshp>=2.3
pyproj>=3.5

# importer_boundaries uses to _convert_ring reprojects (techncially already supplied by shapely)
numpy>=1.22
